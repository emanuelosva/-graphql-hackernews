/**
 * ***********************************************
 * @fileoverview Link types, queries and mutations
 * ***********************************************
 */

const linkTypes = require('./types')
const linkQuery = require('./query')
const linkMutation = require('./mutation')

module.exports = {
  linkTypes,
  linkQuery,
  linkMutation
}
