/**
 * ***********************************************
 * @fileoverview User types, queries and mutations
 * ***********************************************
 */

const userTypes = require('./types')
const userQuery = require('./query')
const userMutation = require('./mutation')

module.exports = {
  userTypes,
  userQuery,
  userMutation,
}
